# Launch assets

- `concept.png` is the generated visual reference used for the launch page.
- `site-screenshot.png` is the browser-rendered screenshot of the implemented site.

The public site is designed to be hosted from the repository root as a static
page. The update forms post to FormSubmit and deliver signups to the repository
owner email configured for this launch.
